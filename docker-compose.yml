services:
    block:
        container_name: block
        image: timefactory/block:dev
        volumes:
            - geth:/.ethereum
            - ./output:/output
        # user: 0:0
        networks:
            - timefactory

networks:
    timefactory:
        external: true
volumes:
    geth:
        external: true
